# Immediate Next Logical Step

## Week 6 Complete — CLI Filter and Count Shipped

Week 6 extended the CLI with `filter` and `count` commands on validated JSON/JSONL files.

Confirm green baseline:

```bash
uv run ruff check .
uv run pytest
```

Commit with message: **Add Week 6 CLI filter and count commands**

## Next Options

- Additional CLI commands (export, review queue)
- Evaluation fixtures and golden-file tests
- Phase 2 FastAPI ingestion (Weeks 9–16 per README roadmap)

## Week 7+ Non-Goals

- No FastAPI yet
- No database
- No LLMs or agents
- No MCP
- No cloud deployment
